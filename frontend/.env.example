# Backend API URL (used for direct API calls outside the Next.js proxy)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Backend URL for Next.js API rewrites (server-side only, not exposed to browser)
BACKEND_URL=http://localhost:3001
